<%= simple_form_for @hospital, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :_type %>
  <%= f.input :_id %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :coordinates %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                hospitals_path, :class => 'btn' %>
  </div>
<% end %>
